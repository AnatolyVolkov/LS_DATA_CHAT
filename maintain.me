# Скачать новую версию кода (если используете git)
git pull https://github.com/AnatolyVolkov/LS_DATA_CHAT

# Пересобрать и перезапустить
docker-compose down
docker-compose build --no-cache
docker-compose up -d

# Проверить логи
docker-compose logs -froot@srv-git-support:/opt/LS_DATA_CHAT# 